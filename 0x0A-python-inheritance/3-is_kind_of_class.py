#!/usr/bin/python3
"""thiss is a program returns attributes and methods of an object"""


def is_kind_of_class(obj, a_class):
    """checks if object is instance of the class or the
    from the inhereted class
    """
    return isinstance(obj, a_class)
