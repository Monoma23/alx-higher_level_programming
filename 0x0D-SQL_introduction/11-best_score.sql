-- listss all records with ah score >= 10 in table second_table
-- display both the score and the name, and databases is passedd as an argumentt of the mysql command
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
